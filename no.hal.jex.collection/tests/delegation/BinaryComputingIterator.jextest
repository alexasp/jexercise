import java.util.Iterator

test delegation.BinaryComputingIterator

instance Iterator<Double> iterator1 = newArrayList(1.0, -2.0).iterator
instance Iterator<Double> iterator2 = newArrayList(5.0, 3.0).iterator


sequence computeResults{
	instance iterator = new BinaryComputingIterator(iterator1, iterator2, (x, y) -> x*y)
	
}